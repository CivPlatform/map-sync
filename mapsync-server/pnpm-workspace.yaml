allowBuilds:
  bufferutil: true

onlyBuiltDependencies:
  - bufferutil

blockExoticSubdeps: true
# prevent packages newer than 1 day old from being installed
minimumReleaseAge: 1440
# https://pnpm.io/settings#trustpolicy
trustPolicy: no-downgrade
